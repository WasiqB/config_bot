require "spec_helper"

describe ConfigBot do
  it "has a version number" do
    expect(ConfigBot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
